en:
  theme_metadata:
    description: "Site-wide auth-aware launch-offer surfaces: a dismissable top banner, a signup-page panel, and an optional one-time full-screen splash (anon + members). Anonymous visitors see the 90-day Business launch offer; logged-in members see their active-Business state (no expiry). Dismissals persist cross-device for members via user fields."
